import React from "react";
import { SafeAreaView, ScrollView } from "react-native";

export const RegisterScreen = () => {

    return(
         
        < SafeAreaView>
         
         <ScrollView>
            

         </ScrollView>
        
        </SafeAreaView>

    );
}