import { StyleSheet } from "react-native";

export const registerStyle = StyleSheet.create({
    content: {
        padding: 20,
        paddingTop: 0
    },

    button: {
        marginTop: 30,
        padding:8
    }


})