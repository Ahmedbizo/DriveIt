import { DefaultTheme } from "react-native-paper";

export const them = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#F9AA33',
        background: "transparent"
    }
}