import { DefaultTheme } from "react-native-paper";

export const them = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#4682b4',
        background: "transparent"
    }
}